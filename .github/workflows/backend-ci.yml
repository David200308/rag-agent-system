name: Backend CI

on:
  push:
    branches: [main]
    paths:
      - 'backend/agent-system-rest/src/**'
      - 'backend/agent-system-rest/pom.xml'
      - 'backend/agent-system-rest/Dockerfile'
      - 'backend/storage-inner/src/**'
      - 'backend/storage-inner/pom.xml'
      - 'backend/storage-inner/Dockerfile'
      - 'backend/notification-consumer/src/**'
      - 'backend/notification-consumer/pom.xml'
      - 'backend/notification-consumer/Dockerfile'
      - 'backend/auth-inner/src/**'
      - 'backend/auth-inner/pom.xml'
      - 'backend/auth-inner/Dockerfile'
      - 'backend/finance-inner/src/**'
      - 'backend/finance-inner/pom.xml'
      - 'backend/finance-inner/Dockerfile'
      - 'backend/travel-inner/src/**'
      - 'backend/travel-inner/pom.xml'
      - 'backend/travel-inner/Dockerfile'
      - 'backend/pom.xml'
      - 'backend/docker-entrypoint.sh'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'backend/agent-system-rest/src/**'
      - 'backend/agent-system-rest/pom.xml'
      - 'backend/agent-system-rest/Dockerfile'
      - 'backend/storage-inner/src/**'
      - 'backend/storage-inner/pom.xml'
      - 'backend/storage-inner/Dockerfile'
      - 'backend/notification-consumer/src/**'
      - 'backend/notification-consumer/pom.xml'
      - 'backend/notification-consumer/Dockerfile'
      - 'backend/auth-inner/src/**'
      - 'backend/auth-inner/pom.xml'
      - 'backend/auth-inner/Dockerfile'
      - 'backend/finance-inner/src/**'
      - 'backend/finance-inner/pom.xml'
      - 'backend/finance-inner/Dockerfile'
      - 'backend/travel-inner/src/**'
      - 'backend/travel-inner/pom.xml'
      - 'backend/travel-inner/Dockerfile'
      - 'backend/pom.xml'
      - 'backend/docker-entrypoint.sh'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME_REST: ${{ github.repository }}/rag-agent-system-backend
  IMAGE_NAME_STORAGE: ${{ github.repository }}/rag-agent-system-storage-inner
  IMAGE_NAME_NOTIFICATION: ${{ github.repository }}/rag-agent-system-notification-consumer
  IMAGE_NAME_AUTH: ${{ github.repository }}/rag-agent-system-auth-inner
  IMAGE_NAME_FINANCE: ${{ github.repository }}/rag-agent-system-finance-inner
  IMAGE_NAME_TRAVEL: ${{ github.repository }}/rag-agent-system-travel-inner

jobs:
  # ── Test ────────────────────────────────────────────────────────────────────
  # Runs from the multi-module reactor root, so it builds/tests
  # agent-system-rest, storage-inner, notification-consumer, auth-inner, finance-inner, and travel-inner in one pass.
  test:
    name: Unit Tests
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run tests with coverage
        working-directory: backend
        run: mvn test -B

      - name: Upload Surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            backend/agent-system-rest/target/surefire-reports/
            backend/storage-inner/target/surefire-reports/
            backend/notification-consumer/target/surefire-reports/
            backend/auth-inner/target/surefire-reports/
            backend/finance-inner/target/surefire-reports/
            backend/travel-inner/target/surefire-reports/
          retention-days: 7

      - name: Upload JaCoCo coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: backend/agent-system-rest/target/site/jacoco/
          retention-days: 7

      - name: Coverage summary
        if: always()
        uses: madrapps/jacoco-report@v1.7.1
        with:
          paths: backend/agent-system-rest/target/site/jacoco/jacoco.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 0
          min-coverage-changed-files: 0
          title: Test Coverage

  # ── Build & Push Docker images ───────────────────────────────────────────────
  docker-rest:
    name: Build & Push Backend Image
    runs-on: ubuntu-latest
    needs: test
    # Run on push/dispatch to main only — skip on pull requests
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Buildx must be set up before login so credentials propagate into the builder
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_REST }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/agent-system-rest/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Disable OCI attestation manifests — GHCR rejects them when using
          # GITHUB_TOKEN, causing the push to fail silently with no image produced.
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-rest
          cache-to: type=gha,mode=max,scope=backend-rest

  docker-storage:
    name: Build & Push Storage Image
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_STORAGE }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/storage-inner/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-storage
          cache-to: type=gha,mode=max,scope=backend-storage

  docker-notification:
    name: Build & Push Notification Image
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_NOTIFICATION }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/notification-consumer/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-notification
          cache-to: type=gha,mode=max,scope=backend-notification

  docker-auth:
    name: Build & Push Auth Image
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_AUTH }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/auth-inner/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-auth
          cache-to: type=gha,mode=max,scope=backend-auth

  docker-finance:
    name: Build & Push Finance Image
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_FINANCE }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/finance-inner/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-finance
          cache-to: type=gha,mode=max,scope=backend-finance

  docker-travel:
    name: Build & Push Travel Image
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_TRAVEL }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/travel-inner/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha,scope=backend-travel
          cache-to: type=gha,mode=max,scope=backend-travel
