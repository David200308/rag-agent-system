// Tailwind v4 uses its own PostCSS plugin (@tailwindcss/postcss).
// No tailwind.config.js needed — configuration is done via CSS @theme blocks.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
