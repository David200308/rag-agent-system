<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.5</version>
        <relativePath/>
    </parent>

    <groupId>com.agentsystem</groupId>
    <artifactId>rag-agent-system</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>rag-agent-system</name>
    <description>RAG Agent System — multi-module aggregator (agent-system-rest, agent-system-storage-inner)</description>

    <modules>
        <module>agent-system-rest</module>
        <module>agent-system-storage-inner</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <!--
            Check https://central.sonatype.com/artifact/org.springframework.ai/spring-ai-bom
            for the latest Spring AI version before building.
        -->
        <spring-ai.version>1.1.0</spring-ai.version>
        <langgraph4j.version>1.7.10</langgraph4j.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <aws-sdk.version>2.29.52</aws-sdk.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
                spring-ai-bom 1.1.x and com.yubico:yubico-util:2.5.3 pull in
                jackson-dataformat-cbor:2.18.5, whose POM references jackson-databind:2.19.5-SNAPSHOT
                which does not exist and breaks Docker offline builds. Pin the BOM and the
                specific CBOR artifact to stable 2.18.2 to prevent the SNAPSHOT from ever
                being resolved.
            -->
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>2.18.2</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-cbor</artifactId>
                <version>2.18.2</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bom</artifactId>
                <version>${resilience4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws-sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases><enabled>false</enabled></releases>
        </repository>
    </repositories>
</project>
